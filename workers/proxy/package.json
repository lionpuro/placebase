{
	"name": "@placebase/proxy",
	"version": "0.0.0",
	"private": true,
	"scripts": {
		"deploy": "wrangler deploy",
		"dev": "wrangler dev",
		"start": "wrangler dev",
		"cf-typegen": "wrangler types",
		"format": "npx prettier --write ."
	},
	"devDependencies": {
		"typescript": "^5.5.2",
		"wrangler": "^4.42.0"
	}
}
